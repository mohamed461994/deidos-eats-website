/**
 * Platform identity for the Deidos Eats marketplace chrome (header, footer, 404,
 * document metadata). This is the PLATFORM level only — each restaurant's brand
 * (name, tagline, logo, hero image) is API-driven and lives inside `/r/:slug`.
 * One platform theme today; per-restaurant theming is deferred (see DESIGN.md).
 */
export const PLATFORM_NAME = 'Deidos Eats'
export const PLATFORM_TAGLINE = 'Great local restaurants, ordered in two minutes and tracked to your door.'
