# Unit-test harness ONLY (vitest loads this in test mode). The test suite
# exercises the in-browser mock (simulated kitchen, localStorage store) so it
# can run offline and deterministically. The running site must always use the
# real API — see .env.development. Never copy this mode into dev or prod.
VITE_API_MODE=mock
